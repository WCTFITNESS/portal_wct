const { gerarRelatorioFrete } = require('../services/freteService');

async function gerarRelatorioFrete(from, to) {
    return await gerarRelatorioFrete(from, to);
}

module.exports = { gerarRelatorioFrete };
